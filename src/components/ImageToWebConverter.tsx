
import React from "react";
import RefactoredImageToWebConverter from "@/features/image-to-web/RefactoredImageToWebConverter";

// This is a wrapper component that helps with the migration
const ImageToWebConverter = () => {
  return <RefactoredImageToWebConverter />;
};

export default ImageToWebConverter;
